--1 + True not all types are compatible
--the goal of static typing is that compiler will catch
--these type errors early on, before we run the programm

--:type not (1==1)
--:type not

--add (x,y) = x + y

--add x y = x +y

-- we define add as a function that takes an integer and returns a function that
--takes another integer and returns and integer

--second xs = head (tail xs)
--swap (x,y) = (y,x)
--pair x y  = (x,y)
--palindrom xs = reverse xs == xs
--twice f x = f (f x)
1:2:3:[5,6,7,8]

